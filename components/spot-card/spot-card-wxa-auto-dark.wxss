/* ================================================================
 * 文件名: spot-card-wxa-auto-dark.wxss
 * 描述: 旅游景点卡片组件的自动深色模式样式文件
 * 版本: 1.0.0
 * 创建日期: 2025-05-07
 * 最后修改: 2025-05-13
 * 作者: Tourism_Management开发团队
 * 
 * 功能说明:
 * - 定义系统自动切换深色模式下的样式
 * - 支持媒体查询自动适配系统颜色模式
 * - 提供手动切换的深色模式类样式
 * ================================================================ */

/* 
此文件为黑暗模式样式，创建时间: 2025/5/7
使用方法：
在 spot-card.wxss 文件末尾引入样式
```
@import "./spot-card-wxa-auto-dark.wxss";
```
 */

/* 系统自动深色模式 - 使用媒体查询检测系统颜色方案 */
@media (prefers-color-scheme: dark) {

  /* ==================== 卡片基本样式深色适配 ==================== */
  /* 卡片容器深色样式 - 降低整体亮度避免夜间刺眼 */
  .spot-card {
    background-color: rgb(17, 17, 17);
    /* 深灰色背景 - 近黑色背景降低亮度和对比度 */
    box-shadow: 0 4rpx 20rpx rgba(0, 0, 0, 0.15);
    /* 阴影效果 - 在深色背景上提供微妙的层次感 */
    border: 1rpx solid rgba(255, 255, 255, 0.05);
    /* 微弱边框 - 提供轻微的边界感 */
  }

  /* ==================== 内容元素深色适配 ==================== */
  /* 景点标题深色样式 - 调整文字亮度保持可读性 */
  .spot-title {
    color: rgb(209, 209, 209);
    /* 浅灰色文字 - 亮度适中，确保在深色背景上可读 */
    text-shadow: 0 0 2rpx rgba(0, 0, 0, 0.2);
    /* 微弱阴影 - 增强文字的清晰度 */
  }

  /* 位置信息深色样式 - 次要信息的颜色适配 */
  .spot-location {
    color: rgb(149, 149, 149);
    /* 灰色文字 - 作为次要信息，亮度略低于主要内容 */
  }

  /* 位置图标深色样式 - 与文字协调统一 */
  .location-icon {
    color: rgb(170, 120, 120);
    /* 淡红色图标 - 保持与浅色模式红色图标的色调，但降低饱和度 */
    opacity: 0.85;
    /* 轻微降低不透明度 - 使图标更柔和 */
  }

  /* 分类标签深色样式 - 降低对比度提升易读性 */
  .category-tag {
    background-color: rgb(40, 40, 40);
    /* 深色背景 - 比卡片略浅，形成微妙的层次感 */
    color: rgb(180, 180, 180);
    /* 浅色文字 - 确保在标签深色背景上清晰可见 */
    border: 1rpx solid rgba(255, 255, 255, 0.05);
    /* 微弱边框 - 增强标签的视觉界限 */
  }

  /* 价格信息深色样式 - 调整价格显示效果 */
  .spot-price {
    color: rgb(180, 180, 180);
    /* 浅灰色文字 - 保持价格信息清晰可读 */
    text-shadow: 0 0 1rpx rgba(0, 0, 0, 0.3);
    /* 轻微文字阴影 - 增强价格信息的可见度 */
  }

  /* 免费价格深色样式 - 区分常规价格和免费标识 */
  .free-price {
    color: rgb(120, 180, 120);
    /* 淡绿色文字 - 保持"免费"标识的绿色色调但降低亮度 */
    opacity: 0.9;
    /* 略微降低不透明度 - 使颜色更柔和 */
  }

  /* ==================== 评分与互动元素深色适配 ==================== */
  /* 评分信息深色样式 - 调整评分元素的可见性 */
  .spot-rating {
    color: rgb(160, 160, 160);
    /* 灰色文字 - 提供适当的对比度 */
  }

  /* 评分星星图标深色样式 - 调整星星颜色适应深色环境 */
  .rating-star {
    color: rgb(230, 180, 40);
    /* 暗金色 - 降低黄色星星的亮度，但保持辨识度 */
    opacity: 0.9;
    /* 轻微降低不透明度 - 减少视觉刺激 */
  }

  /* 评分徽章深色样式 - 调整徽章背景以适应深色环境 */
  .rating-badge {
    background-color: rgba(0, 0, 0, 0.7);
    /* 深色半透明背景 - 增加不透明度 */
    color: rgb(220, 220, 220);
    /* 亮灰色文字 - 增强对比度提高可读性 */
  }
}

/* 手动深色模式类样式 - 用于通过JS手动切换深色模式 */
/* ==================== 卡片基本元素手动深色样式 ==================== */
/* 卡片容器手动深色样式 - 与系统自动深色模式保持统一 */
.spot-card.dark-mode {
  background-color: rgb(17, 17, 17) !important;
  /* 深色背景，强制优先级 - 确保覆盖任何其他样式 */
  box-shadow: 0 4rpx 20rpx rgba(0, 0, 0, 0.15) !important;
  /* 阴影效果，强制优先级 - 提供微妙的立体感 */
  border: 1rpx solid rgba(255, 255, 255, 0.05) !important;
  /* 微弱边框，强制优先级 - 界定卡片边界 */
}

/* 卡片内容区域手动深色样式 - 保持与卡片背景一致 */
.spot-content.dark-mode {
  background-color: rgb(17, 17, 17) !important;
  /* 深色背景，强制优先级 - 与卡片容器颜色一致 */
}

/* 景点标题手动深色样式 - 确保文字在深色背景上清晰可见 */
.spot-title.dark-mode {
  color: rgb(209, 209, 209) !important;
  /* 浅色文字，强制优先级 - 提供足够对比度 */
  text-shadow: 0 0 2rpx rgba(0, 0, 0, 0.2) !important;
  /* 微弱阴影，强制优先级 - 增强文字清晰度 */
}

/* ==================== 卡片次要元素手动深色样式 ==================== */
/* 位置信息手动深色样式 - 调整次要信息的视觉强度 */
.spot-location.dark-mode {
  color: rgb(149, 149, 149) !important;
  /* 灰色文字，强制优先级 - 比主标题更低的亮度 */
}

/* 位置图标手动深色样式 - 确保图标与文字协调 */
.location-icon.dark-mode {
  color: rgb(170, 120, 120) !important;
  /* 淡红色图标，强制优先级 - 降低红色的饱和度 */
  opacity: 0.85 !important;
  /* 不透明度，强制优先级 - 使图标更柔和 */
}

/* ==================== 卡片辅助元素手动深色样式 ==================== */
/* 分类标签手动深色样式 - 提供适当的视觉区分 */
.category-tag.dark-mode {
  background-color: rgb(40, 40, 40) !important;
  /* 深色背景，强制优先级 - 比卡片背景略浅 */
  color: rgb(180, 180, 180) !important;
  /* 浅色文字，强制优先级 - 提供可读性 */
  border: 1rpx solid rgba(255, 255, 255, 0.05) !important;
  /* 微弱边框，强制优先级 - 增强标签边界 */
}

/* 页脚区域手动深色样式 - 设置卡片底部区域样式 */
.spot-footer.dark-mode {
  border-top-color: rgba(255, 255, 255, 0.05) !important;
  /* 边框颜色，强制优先级 - 微弱的分隔线 */
  background-color: rgb(24, 24, 24) !important;
  /* 背景色，强制优先级 - 略深于卡片主体，增加层次感 */
}

/* ==================== 价格和评分手动深色样式 ==================== */
/* 价格信息手动深色样式 - 确保价格信息清晰可见 */
.spot-price.dark-mode {
  color: rgb(180, 180, 180) !important;
  /* 浅色文字，强制优先级 - 提供足够对比度 */
  text-shadow: 0 0 1rpx rgba(0, 0, 0, 0.3) !important;
  /* 轻微文字阴影，强制优先级 - 增强文字清晰度 */
}

/* 免费价格手动深色样式 - 保持"免费"的特殊标识 */
.free-price.dark-mode {
  color: rgb(120, 180, 120) !important;
  /* 淡绿色文字，强制优先级 - 降低绿色亮度但保持辨识度 */
  opacity: 0.9 !important;
  /* 不透明度，强制优先级 - 使颜色更柔和 */
}

/* 评分信息手动深色样式 - 调整评分显示 */
.spot-rating.dark-mode {
  color: rgb(160, 160, 160) !important;
  /* 灰色文字，强制优先级 - 提供适当对比度 */
}

/* 评分星星图标手动深色样式 - 调整星星颜色 */
.rating-star.dark-mode {
  color: rgb(230, 180, 40) !important;
  /* 暗金色，强制优先级 - 降低黄色星星亮度 */
  opacity: 0.9 !important;
  /* 不透明度，强制优先级 - 减少视觉刺激 */
}

/* 评分徽章手动深色样式 - 调整徽章背景 */
.rating-badge.dark-mode {
  background-color: rgba(0, 0, 0, 0.7) !important;
  /* 深色半透明背景，强制优先级 - 增加不透明度 */
  color: rgb(220, 220, 220) !important;
  /* 亮灰色文字，强制优先级 - 增强对比度 */
}